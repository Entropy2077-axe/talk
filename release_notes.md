# Talk v0.1.52

本次更新将存档系统重构为“以世界为核心”，同时合入新的聊天 Agent 工具调用流程。

## 主要更新

- “存档与回档”现在按世界管理：支持世界列表、手动/自动存档筛选、批量删除、存档详情、重命名和一键调用。
- 调用存档前自动保存当前世界；内容完全相同的自动存档只更新时间，不重复占用空间。
- 世界存档覆盖联系人、聊天、群聊、朋友圈、关系、记忆、经历、地点与生活模拟等完整状态。
- 旧版本联系人会按原来绑定的世界自动迁移并分别生成升级存档；新建世界立即生成初始自动存档。
- 原“发现 → 世界观”入口合并到“存档与回档”，联系人不再单独选择或切换世界。
- 通用设置新增“各世界独立保存经济与资产”开关；默认关闭，余额、仓库、职业和商城记录跨世界共享。
- 私聊和群聊改用结构化 Agent 工具调用发送文本、图片、表情、日程及经济动作，减少协议转换错误。
- 聊天生成支持用户随时停止，不再因为固定 30 秒超时被强制中断。

## 数据兼容

- IndexedDB 升级到 v39，新增完整世界快照表。
- 备份格式升级到 v9，并继续兼容 v1–v8 备份。
- 旧联系人剧情线和资源存档继续保留在本地，避免升级时丢失历史数据。

## 验证

- TypeScript、UI 组件规范、完整单元测试和生产构建通过。
- Android APK 与 Windows 安装包使用空 API Key 构建，并在上传前完成敏感值扫描。

## 下载

- `Talk-0.1.52-android-debug.apk`：Android 调试签名安装包。
- `Talk-0.1.52-x64.exe`：Windows x64 安装版。
- `Talk-0.1.52-portable-x64.exe`：Windows x64 便携版。
